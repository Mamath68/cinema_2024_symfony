<!DOCTYPE html>
		<html class="bg-gray-100">
			<head>
				<meta charset="UTF-8">
				<title>
					{% block title %}
						Welcome!
					{% endblock %}
				</title>
				<link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.4em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
				{% block stylesheets %}
					<link rel="stylesheet" href="{{ asset('styles/app.css') }}">
				{% endblock %}

				{% block javascripts %}
					{% block importmap %}
						{{ importmap('app') }}
					{% endblock %}
					<script src="{{asset('js/script.js')}}"></script>
				{% endblock %}

			</head>
			<body>
				<nav class="bg-white shadow-md">
					<div class="px-2 mx-auto max-w-7xl sm:px-6 lg:px-8">
						<div class="relative flex items-center justify-between h-16">
							<div
								class="absolute inset-y-0 left-0 flex items-center sm:hidden">
								<!-- Mobile menu button-->
								<button id="menu-button" class="inline-flex items-center justify-center p-2 text-gray-400 rounded-md hover:text-white hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-white" aria-expanded="false">
									<span class="sr-only">
										Open main menu
									</span>
									<!-- Icon when menu is closed. -->
									<svg class="block w-6 h-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewbox="0 0 24 24" stroke="currentColor" aria-hidden="true">
										<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
									</svg>
									<!-- Icon when menu is open. -->
									<svg class="hidden w-6 h-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewbox="0 0 24 24" stroke="currentColor" aria-hidden="true">
										<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
									</svg>
								</button>
							</div>
							<div class="flex items-center justify-center flex-1 sm:items-stretch sm:justify-start">
								<div class="flex-shrink-0">
									<a href="{{ path('app_home') }}" class="text-2xl font-bold text-gray-900">
										Projet Elan 2024
									</a>
								</div>
								<div class="hidden sm:block sm:ml-6">
									<div class="flex space-x-4">
										<a href="{{ path('app_home') }}" class="{{ app.request.attributes.get('_route') == 'app_home' ? 'bg-gray-700 text-white ' : ' text-gray-900 hover:bg-[#007bff] hover:text-white' }} px-3 py-2 text-sm font-medium rounded-md">
											Home
										</a>
										<a href="{{ path('app_movies') }}" class="{{ app.request.attributes.get('_route') == 'app_movies' ? 'bg-gray-700 text-white ' : ' text-gray-900 hover:bg-[#007bff] hover:text-white' }} px-3 py-2 text-sm font-medium rounded-md">
											Movies
										</a>
										<a href="{{ path('app_genres') }}" class="{{ app.request.attributes.get('_route') == 'app_genres' ? 'bg-gray-700 text-white ' : ' text-gray-900 hover:bg-[#007bff] hover:text-white' }} px-3 py-2 text-sm font-medium rounded-md">
											Genres
										</a>
										<a href="{{ path('app_actors') }}" class="{{ app.request.attributes.get('_route') == 'app_actors' ? 'bg-gray-700 text-white ' : ' text-gray-900 hover:bg-[#007bff] hover:text-white' }} px-3 py-2 text-sm font-medium rounded-md">
											Acteurs
										</a>
										<a href="{{ path('app_realisateurs') }}" class="{{ app.request.attributes.get('_route') == 'app_realisateurs' ? 'bg-gray-700 text-white ' : ' text-gray-900 hover:bg-[#007bff] hover:text-white' }} px-3 py-2 text-sm font-medium rounded-md">
											Réalisateurs
										</a>
									</div>
								</div>
							</div>

						</div>
					</div>
					<!-- Mobile menu, show/hide based on menu state. -->
					<div id="mobile-menu" class="hidden sm:hidden">
						<div class="px-2 pt-2 pb-3 space-y-1">
							<a href="{{ path('app_home') }}" class="block px-3 py-2 text-base font-medium text-gray-900 rounded-md hover:bg-gray-700 hover:text-white">
								Home
							</a>
							<a href="{{ path('app_movies') }}" class="block px-3 py-2 text-base font-medium text-gray-900 rounded-md hover:bg-gray-700 hover:text-white">
								Movies
							</a>
							<a href="{{ path('app_genres') }}" class="block px-3 py-2 text-base font-medium text-gray-900 rounded-md hover:bg-gray-700 hover:text-white">
								Genres
							</a>
							<a href="{{ path('app_realisateurs') }}" class="block px-3 py-2 text-base font-medium text-gray-900 rounded-md hover:bg-gray-700 hover:text-white">
								Réalisateurs
							</a>
						</div>
					</div>
				</nav>
				<main>
					{% block body %}
					{% endblock %}
				</main>
				<footer class="py-4 text-center bg-gray-200">
					<h1>
						Footer
					</h1>
				</footer>
			</body>

		</html>
